apiVersion: apps/v1
kind: Deployment
metadata:
  name: oyveybot
  namespace: discord-bots
  labels:
    app: oyveybot
    language: nodejs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oyveybot
  template:
    metadata:
      labels:
        app: oyveybot
        language: nodejs
    spec:
      containers:
      - name: oyveybot
        image: localhost:5000/oyveybot:latest
        imagePullPolicy: Always
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        volumeMounts:
        - name: auth
          mountPath: /app/auth.json
          subPath: auth.json
          readOnly: true
      volumes:
      - name: auth
        secret:
          secretName: oyveybot-secret
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: oyveybot
  namespace: discord-bots
spec:
  selector:
    app: oyveybot
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP

apiVersion: apps/v1
kind: Deployment
metadata:
  name: tarotbot
  namespace: discord-bots
  labels:
    app: tarotbot
    language: nodejs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tarotbot
  template:
    metadata:
      labels:
        app: tarotbot
        language: nodejs
    spec:
      containers:
      - name: tarotbot
        image: localhost:5000/tarotbot:latest
        imagePullPolicy: Always
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        volumeMounts:
        - name: auth
          mountPath: /app/auth.json
          subPath: auth.json
          readOnly: true
      volumes:
      - name: auth
        secret:
          secretName: tarotbot-secret
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: tarotbot
  namespace: discord-bots
spec:
  selector:
    app: tarotbot
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP

apiVersion: apps/v1
kind: Deployment
metadata:
  name: purpleharobot
  namespace: discord-bots
  labels:
    app: purpleharobot
    language: python
spec:
  replicas: 1
  selector:
    matchLabels:
      app: purpleharobot
  template:
    metadata:
      labels:
        app: purpleharobot
        language: python
    spec:
      containers:
      - name: purpleharobot
        image: localhost:5000/purpleharobot:latest
        imagePullPolicy: Always
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        volumeMounts:
        - name: auth
          mountPath: /app/auth.txt
          subPath: auth.txt
          readOnly: true
      volumes:
      - name: auth
        secret:
          secretName: purpleharobot-secret
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: purpleharobot
  namespace: discord-bots
spec:
  selector:
    app: purpleharobot
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP

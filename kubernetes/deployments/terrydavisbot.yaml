apiVersion: apps/v1
kind: Deployment
metadata:
  name: terrydavisbot
  namespace: discord-bots
  labels:
    app: terrydavisbot
    language: nodejs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: terrydavisbot
  template:
    metadata:
      labels:
        app: terrydavisbot
        language: nodejs
    spec:
      containers:
      - name: terrydavisbot
        image: localhost:5000/terrydavisbot:latest
        imagePullPolicy: Always
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        volumeMounts:
        - name: auth
          mountPath: /app/auth.json
          subPath: auth.json
          readOnly: true
      volumes:
      - name: auth
        secret:
          secretName: terrydavisbot-secret
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: terrydavisbot
  namespace: discord-bots
spec:
  selector:
    app: terrydavisbot
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP

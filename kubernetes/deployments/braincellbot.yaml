apiVersion: apps/v1
kind: Deployment
metadata:
  name: braincellbot
  namespace: discord-bots
  labels:
    app: braincellbot
    language: csharp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: braincellbot
  template:
    metadata:
      labels:
        app: braincellbot
        language: csharp
    spec:
      containers:
      - name: braincellbot
        image: localhost:5000/braincellbot:latest
        imagePullPolicy: Always
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        volumeMounts:
        - name: auth
          mountPath: /app/auth.txt
          subPath: auth.txt
          readOnly: true
      volumes:
      - name: auth
        secret:
          secretName: braincellbot-secret
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: braincellbot
  namespace: discord-bots
spec:
  selector:
    app: braincellbot
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP

apiVersion: apps/v1
kind: Deployment
metadata:
  name: manganotifier
  namespace: discord-bots
  labels:
    app: manganotifier
    language: csharp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: manganotifier
  template:
    metadata:
      labels:
        app: manganotifier
        language: csharp
    spec:
      containers:
      - name: manganotifier
        image: localhost:5000/manganotifier:latest
        imagePullPolicy: Always
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        volumeMounts:
        - name: auth
          mountPath: /app/auth.txt
          subPath: auth.txt
          readOnly: true
      volumes:
      - name: auth
        secret:
          secretName: manganotifier-secret
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: manganotifier
  namespace: discord-bots
spec:
  selector:
    app: manganotifier
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP

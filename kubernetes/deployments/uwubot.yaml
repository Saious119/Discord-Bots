apiVersion: apps/v1
kind: Deployment
metadata:
  name: uwubot
  namespace: discord-bots
  labels:
    app: uwubot
    language: nodejs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: uwubot
  template:
    metadata:
      labels:
        app: uwubot
        language: nodejs
    spec:
      containers:
      - name: uwubot
        image: localhost:5000/uwubot:latest
        imagePullPolicy: Always
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        volumeMounts:
        - name: auth
          mountPath: /app/auth.json
          subPath: auth.json
          readOnly: true
      volumes:
      - name: auth
        secret:
          secretName: uwubot-secret
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: uwubot
  namespace: discord-bots
spec:
  selector:
    app: uwubot
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
